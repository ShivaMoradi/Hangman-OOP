export default class Question{
  constructor ( word ) {
    this.word = word
    this.answer = this.promptWord.toUpperCase
  }
  promptWord () {
  
    let word = prompt( "Type the secret word: " )
}

}
